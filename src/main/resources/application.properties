spring.application.name=movie_list_rest_api

spring.datasource.url=jdbc:mysql://localhost:3307/movie_list
spring.datasource.username=root
spring.datasource.password=rootpassword
#spring.jpa.defer-datasource-initialization=true

#spring.jpa.hibernate.ddl-auto=validate
# validates the schema, makes no changes to the database

spring.jpa.hibernate.ddl-auto=update
# update the schema

#spring.jpa.hibernate.ddl-auto=create
# creates the schema, destroying previous data

#spring.jpa.hibernate.ddl-auto=create-drop
# drop the schema when the session is closed

#spring.jpa.hibernate.ddl-auto=none
# makes no changes to the database
